# Check out LF everywhere so content hashes match across platforms.
* text=auto eol=lf

# Preserve downloaded corpus bytes so content-versioned hashes remain stable.
data/gutenberg/*.txt -text

output/pdf/*.pdf binary
