# Focused architecture concepts

Four concept images based on GitHub issue 58 and its local source images. Generated with the built-in image_gen tool. Status labels describe the issue snapshot at d62a9ad, not a fresh implementation audit.

Source: https://github.com/DesmondChoy/linger/issues/58#issuecomment-5740739683

## View 1

Use case: infographic-diagram. Generate ONE finished landscape architecture diagram, 1536x1024 or higher with same 3:2 aspect ratio, part of a consistent four-page set for Linger. The purpose is to REDUCE CROWDING. Restrained flat editorial technical drawing, warm white background #FAFAF7, dark ink #172B3A, teal #287D85 for implemented components, muted amber #B77526 for proposed changes. Huge whitespace, generous 64px margins, clean sans-serif typography, large title, readable labels, very thin lines. No gradients, shadows, decorative illustrations, icons, 3D, or watermark. Maximum two short lines per normal box. Solid teal outline and pale teal fill mean existing, amber DASHED outline and very pale amber fill mean proposed. Dark text everywhere. Every arrow has a single unmistakable endpoint. No arrow crossings. Avoid enclosing every region in another box. Use small section labels and whitespace. Header has page number and title, plus small shared legend 'Solid = existing' and 'Dashed = proposed'. Footer text EXACT 'Issue #58 snapshot • d62a9ad • 19 Sep 2026' and 'Conceptual view • selected relationships'. This describes the issue snapshot, not a new audit or implementation. Status distinctions must not rely on colour alone. Render only the specified text; no extra detail.
PAGE 01 title 'How a conversation works'
Subtitle 'The application controls each handoff and the final response.'
COMPOSITION: Top quarter small entry line 'Reader' → solid box 'React UI + FastAPI'. An amber dashed annotation below this entry box, linked with a short dashed line, reads 'PROPOSED' then 'Sign-in + account checks' then 'If multiple accounts are retained'. Keep these in leftmost top region without crowding the main flow.
Middle prominent horizontal sequence inside ONE airy boundary titled 'Application-owned workflow'. Four solid boxes, left to right: 'Provenance' / 'Emotional preflight' → 'Muse' / 'Draft or revise' → 'Provenance' / 'Review candidate' → 'Application checks' / 'Release, clarify or decline'. Entry line comes from React UI + FastAPI into left side of this boundary. An output arrow from last box to a small text label 'Response to reader' at right, not a long return loop.
Directly BELOW the Muse box, a short line leads into label 'Application tool adapters', then splits into just TWO horizontally arranged solid cards in the lower middle of the page: 'Librarian' / 'Scoped book evidence'; 'Serendipity' / 'Memory and connections'. Under Librarian in plain text 'Versioned books + scoped retrieval'. Under Serendipity in plain text 'Permitted memories • scoped books • granted web'. These are subordinate sources, not extra boxes. Cards positioned far enough apart for all text.
Bottom left small note 'Preflight can stop the turn. At most one revision.' Bottom right small note 'Session history is held in server memory.' Show a compact horizontal quiet bottom strip 'Shared model provider for the five agent roles'. Do not connect every agent to this strip.
IMPORTANT: Provenance appears twice because it performs two tasks; it is not two separate agents. Do not add peer-to-peer networking. No Sculptor in chat. Proposed account controls apply at the app entry. No CI, deployment, logs or memory-curation workflow here.

## View 2

Use case: infographic-diagram. Generate ONE finished landscape architecture diagram, 1536x1024 or higher with same 3:2 aspect ratio, part of a consistent four-page set for Linger. The purpose is to REDUCE CROWDING. Restrained flat editorial technical drawing, warm white background #FAFAF7, dark ink #172B3A, teal #287D85 for implemented components, muted amber #B77526 for proposed changes. Huge whitespace, generous 64px margins, clean sans-serif typography, large title, readable labels, very thin lines. No gradients, shadows, decorative illustrations, icons, 3D, or watermark. Maximum two short lines per normal box. Solid teal outline and pale teal fill mean existing, amber DASHED outline and very pale amber fill mean proposed. Dark text everywhere. Every arrow has a single unmistakable endpoint. No arrow crossings. Avoid enclosing every region in another box. Use small section labels and whitespace. Header has page number and title, plus small shared legend 'Solid = existing' and 'Dashed = proposed'. Footer text EXACT 'Issue #58 snapshot • d62a9ad • 19 Sep 2026' and 'Conceptual view • selected relationships'. This describes the issue snapshot, not a new audit or implementation. Status distinctions must not rely on colour alone. Render only the specified text; no extra detail.
PAGE 02 title 'How memory changes are reviewed'
Subtitle 'Curation is a separate callable workflow.'
Upper middle of canvas a single generously spaced horizontal chain of FIVE solid boxes:
'Memory service' / 'Select originals' →
'Sculptor' / 'Propose changes' →
'Provenance' / 'Review proposal' →
'Application + policy' / 'Validate and apply' →
'Curated view' / 'Preserve originals'.
Below that chain one simple teal sentence 'Approved changes are audited. Original records are preserved.'
Lower middle an amber dashed large panel entitled 'PROPOSED · Test the complete capture-to-curation journey'. Inside three clean steps left-to-right connected with dashed arrows: 'Capture + curation replay' → 'Real Provenance review' → 'Assert rejected changes are not saved'. All parts amber dashed. Small subtitle INSIDE panel 'Replace the always-approving reviewer in this evaluation runner.' ONE short dotted vertical leader from the panel's top to the solid Provenance box above, labeled 'Test the existing review boundary'. Do not connect the proposed replay as if it is another runtime stage.
Bottom of page two brief calm notes, not boxes:
'Chat does not trigger Sculptor. Interactive capture is off by default.'
'Automatic capture → curation → later surfacing remains incomplete.'
No other architecture components. Existing runtime Provenance review is solid and unambiguously implemented. Proposed real reviewer applies specifically to capture_curation_replay; do not imply all evaluation runners lack production Provenance.

## View 3

Use case: infographic-diagram. Generate ONE finished landscape architecture diagram, 1536x1024 or higher with same 3:2 aspect ratio, part of a consistent four-page set for Linger. The purpose is to REDUCE CROWDING. Restrained flat editorial technical drawing, warm white background #FAFAF7, dark ink #172B3A, teal #287D85 for implemented components, muted amber #B77526 for proposed changes. Huge whitespace, generous 64px margins, clean sans-serif typography, large title, readable labels, very thin lines. No gradients, shadows, decorative illustrations, icons, 3D, or watermark. Maximum two short lines per normal box. Solid teal outline and pale teal fill mean existing, amber DASHED outline and very pale amber fill mean proposed. Dark text everywhere. Every arrow has a single unmistakable endpoint. No arrow crossings. Avoid enclosing every region in another box. Use small section labels and whitespace. Header has page number and title, plus small shared legend 'Solid = existing' and 'Dashed = proposed'. Footer text EXACT 'Issue #58 snapshot • d62a9ad • 19 Sep 2026' and 'Conceptual view • selected relationships'. This describes the issue snapshot, not a new audit or implementation. Status distinctions must not rely on colour alone. Render only the specified text; no extra detail.
PAGE 03 title 'How a version reaches release'
Subtitle 'Extend existing tests and reviewed evaluations into a release path.'
THREE horizontal rows, stacked vertically with plenty of space, and connectors arranged with no crossings.
ROW ONE small label 'Build and security'. Four boxes horizontally: solid 'Versioned repository' / 'Code, skills and books' → solid 'Automated tests' / 'GitHub Actions' → amber dashed 'PROPOSED' / 'Frontend build + lint' → amber dashed 'PROPOSED' / 'Security checks'. Under security box two plain short lines 'Dependency and code scans' and 'Retrieved-content attack tests'. Under automated tests small plain note 'Unit tests block AI provider calls.'
ROW TWO label 'Behavioural evidence'. Four boxes horizontally: solid 'Human review' → solid 'Approved evaluation data' / 'Approval tied to exact files' → solid 'Scenario replay' → amber dashed 'PROPOSED' / 'Release behaviour checks'. Below last box small text 'Real-model results + human judgement' and 'Repeat key cases • separate holdout'.
ROW THREE an airy amber dashed enclosure labelled 'PROPOSED · Deployment and recovery'. Inside four dashed boxes horizontally: 'Versioned package' → 'Clean test environment' → 'Release approval' → 'Deploy'. Below test environment plain text 'Book answer + refusal smoke checks'. Below Deploy plain text 'Practise rollback'.
The build/security row and behavioural-evidence row both feed proposed Release approval, using a single clean vertical rail in the right-hand whitespace with short branches. This means both gates are required, not that CI currently invokes human review. Do NOT connect automated tests to Human review. Do NOT turn the current rows into a claimed existing deployment pipeline.
Bottom small note 'Package the app with its books and local models.'
Keep the overall page visually spare despite three rows. 14 compact boxes maximum, bold hierarchy and large whitespace. No app runtime details, memory pipeline, observability infrastructure or other services.

## View 4

Use case: infographic-diagram. Generate ONE finished landscape architecture diagram, 1536x1024 or higher with same 3:2 aspect ratio, part of a consistent four-page set for Linger. The purpose is to REDUCE CROWDING. Restrained flat editorial technical drawing, warm white background #FAFAF7, dark ink #172B3A, teal #287D85 for implemented components, muted amber #B77526 for proposed changes. Huge whitespace, generous 64px margins, clean sans-serif typography, large title, readable labels, very thin lines. No gradients, shadows, decorative illustrations, icons, 3D, or watermark. Maximum two short lines per normal box. Solid teal outline and pale teal fill mean existing, amber DASHED outline and very pale amber fill mean proposed. Dark text everywhere. Every arrow has a single unmistakable endpoint. No arrow crossings. Avoid enclosing every region in another box. Use small section labels and whitespace. Header has page number and title, plus small shared legend 'Solid = existing' and 'Dashed = proposed'. Footer text EXACT 'Issue #58 snapshot • d62a9ad • 19 Sep 2026' and 'Conceptual view • selected relationships'. This describes the issue snapshot, not a new audit or implementation. Status distinctions must not rely on colour alone. Render only the specified text; no extra detail.
PAGE 04 title 'How operations improve the next release'
Subtitle 'Build on existing records, timing data and privacy-aware logs.'
COMPOSITION three roomy horizontal lanes with whitespace between them. Each has one existing anchor at LEFT and proposed local extension on the RIGHT. No connectors between lanes.
LANE ONE label 'Reproduce a result'. Solid teal box 'Existing version records' / 'Source hashes + skill fingerprints' → amber dashed box 'PROPOSED · One version summary per run' / 'Code, settings, models, prompts, books and results'. Short plain text under amber box 'Link reviewed cases and results to the released version.'
LANE TWO label 'Check operating limits'. Solid teal box 'Runtime measurements' / 'Response times and metadata' → amber dashed box 'PROPOSED · Agreed operating limits' / 'Latency, cost and failure thresholds'. Two short plain lines below amber box 'Measure startup separately. Flag releases that exceed limits.' and 'If multiple accounts are retained, test five concurrent sessions.'
LANE THREE label 'Keep a failure from returning'. Solid teal box 'Privacy-aware runtime logs' / 'Metadata only' → amber dashed box 'PROPOSED · Reviewed regression case' / 'Fictional example → human review → regression test'. Short plain text below amber box 'Link the failure, reviewed expectation and affected version.'
BOTTOM full-width quiet typography-only privacy note, separated by a thin neutral line:
'Runtime logs exclude private chat text.'
'Synthetic evaluation logs use a separate path.'
No raw private text flowing into test cases. No implication failures are automatically converted or already integrated. No new monitoring service, data platform, queue, training loop or infrastructure. Preserve enormous whitespace and just SIX primary boxes.

