[project]
name = "linger"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "polars>=1.43.2",
    "requests>=2.34.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "jupyterlab>=4.6.2",
]
