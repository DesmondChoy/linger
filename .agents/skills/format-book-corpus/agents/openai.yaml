interface:
  display_name: "Format Book Corpus"
  short_description: "Format books and optionally enable them in Linger"
  default_prompt: "Use $format-book-corpus to format this book as validated canonical Markdown and a routing catalog, and enable it for Librarian and Reader if requested."
