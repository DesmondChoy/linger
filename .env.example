# Copy to `.env` and fill in. `.env` is gitignored — never commit real keys.
#
#   cp .env.example .env

# --- Model provider -------------------------------------------------------
# Set the key for the provider selected by LINGER_MODEL. The other keys may
# remain empty.
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Model the chat agent runs on. Supported prefixes: google, openai, anthropic.
LINGER_MODEL=google:gemini-2.5-flash

# --- Backend --------------------------------------------------------------
# Browser origins allowed to call the API. Comma-separated.
# 5173 is the Vite dev server default.
LINGER_ALLOWED_ORIGINS=http://localhost:5173

# --- Telemetry (optional) -------------------------------------------------
# Leave unset to disable tracing. Set to a Logfire write token to send
# PydanticAI agent traces to https://logfire.pydantic.dev
# LOGFIRE_TOKEN=
