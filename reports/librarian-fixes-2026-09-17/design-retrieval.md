# Private retrieval recall and complete reranker inputs

The private judge currently sees only the fusion leader plus four reranker winners. Keller's canary passage ranks second lexically and fourth in fusion, but twelfth after reranking; it disappears before ambiguity can be judged. Retain the exact-ID union of the ten lexical and ten semantic candidates, with a fixed twenty-record private budget. Interleave lexical, fusion, semantic, and reranker ranks for ordering. Do not remove overlapping records privately: overlap does not imply that both excerpts contain the same decisive sentence. Public retrieval retains its score threshold, overlap deduplication, and requested result count. Canonical IDs, excerpts, source lines, and scope checks remain unchanged.

FastEmbed's configured tokenizer truncates paired inputs at its model context length. Wrap its existing encoder with `TokenWindowReranker(encoder)` in `apps/backend/rerank_windows.py`, exposing the same `rerank(query, documents)` call. Use the installed Hugging Face Tokenizers overflow and character-offset APIs, cloned from the actual encoder tokenizer. Score all overlapping text windows with a complete query when it fits; for a long query score all overlapping query windows as well. Reserve the tokenizer's actual special-token budget, verify every pair fits without truncation, and aggregate the maximum score back to each original document. Inputs that fit remain unchanged. No second model, provider, or dependency is introduced.

Verify candidate loss with adversarial local scores, exact canonical evidence and private/public separation; verify tail evidence, long-query coverage, pair limits, and unchanged short inputs with a small real Tokenizers tokenizer. Run existing retrieval tests, then cached real-model Keller and long-passage probes with network disabled. Benchmark can use the same wrapper around its existing encoder. These changes improve available evidence; they do not grant disclosure or replace semantic assessment.

## Verification

Failing-before: the private-retention regression lost one of six distinct records; the raw encoder control discarded a decisive tail. Window tests initially failed with the missing helper, then caught incomplete overflow traversal during implementation. The final helper uses `Encoding.truncate` and its complete overflow list, with Unicode/WordPiece and intermediate-window checks.

Passing-after: 49 tests and 11 subtests across hybrid retrieval, private judgement, token windows, boundary planning, and boundary inference. `retrieval-tests.log` records the command outcome. The cached Keller probe retains both canary (private rank 5, relevance 0.000559) and crab (rank 2), with every canonical record unchanged. The private pool contains 18 records. A 1,022-token query/passage pair is scored through three windows under the existing 512-token limit; its semantic score remains low, so this is coverage evidence rather than a sufficiency claim. Exact probe output is in `retrieval-probe.json` and `.log`. No provider calls were made.

An additional route integration run had 53 passes and 13 subtests, with seven failures at Provenance review after successful scoped retrieval. These were reported to the parent for the concurrent Provenance caller migration; no out-of-scope files were changed here.
