# Iteration 13: claim-mapping diagnostic

The final **4/4** is credible, but it is not four correct first attempts. Both negative controls initially received false approvals. Source-bound excerpt validation and inventory feedback rejected those outputs, and each second attempt reached the correct semantic rejection. The two positive controls passed, one after an inventory repair. No provider or gate error occurred.

Evidence: `suite-mapping.json`, all four `diagnostics[].review_input`, complete `model_messages` including output attempts and retry feedback, final `review`, and per-case grades. Model: `openai:gpt-5.6-luna`. Case set digest: `05e3b04ab9911c3ae12a14f3dce90093ca1b1af5b5d1cd531d08c152011f3cbb`. Local revalidation of all four final reviews against their recorded Provenance inputs returned no errors. This was an offline check, not a new model run.

| Case | Attempts | Actual behavior and semantic judgment |
| --- | ---: | --- |
| Wrong record, expected revise | 2 | First `pass` borrowed “I can’t remember things as I used—and I don’t keep the same size for ten minutes together!” for the earlier Chapter 5 record. The validator rejected the excerpt because it is absent from that named source. Second `revise` correctly says the declared record ends before both the memory and verse reports, and requests the later record or removal. Credible rejection after repair. |
| Correct record, expected pass | 2 | First `pass` listed member `(0,0)` twice, once for memory and once for verse. Inventory validation rejected the duplication. Second `pass` uses one authentic continuous excerpt containing both statements. The declared later record supports both clauses. Credible approval after structural repair. |
| Missing joint contribution, expected revise | 2 | First `pass` duplicated member `(0,0)` and supplied a later verse excerpt under the earlier identity record. Feedback reports both the duplicate and wrong-source excerpt. Second keeps the authentic identity contribution but marks the entire claim unsupported: the verse clause needs the later record. This is a real correction of the false semantic approval, unlike iteration 12's accepted false pass after excerpt repair. |
| Complete joint contribution, expected pass | 1 | Both declared records contribute authentic source-local excerpts; the earlier record supplies identity/size confusion and the later record supplies memory/verse. The later private excerpt itself ends before the verse, but the named full canonical record contains it. The full group is supported. Credible approval. |

The relevant canonical IDs are `pg11-v01b38ea4-ch05-ln0960-1016` (earlier identity dialogue) and `pg11-v01b38ea4-ch05-ln1004-1056` (later memory/verse dialogue). Availability of the latter in the input does not authorize an earlier-record declaration to borrow its content. Both final negative reviews preserve this distinction.

Authoritative inputs—context, book and connection sources, session lines, tool outcomes, candidate, current Line, and prior review—match iteration 12 for all four cases. Computed claim-group projections now contain occurrence coverage and `direct` membership; equality of the entire serialized input is therefore not claimed. Neither expectations nor source assignments were changed.

The new diagnostic envelope reports `status=success`, `usage_complete=true`, null failure category/status/kind, and 2/2/2/1 requests. It preserves the raw output attempts needed to distinguish schema/source repair from a correct first judgment. These successful cases do not exercise its failed-run capture branch live. The model still attempted source borrowing twice; deterministic enforcement prevented acceptance in this run, but authentic excerpts alone do not mechanically prove a support summary. No claim of general semantic reliability follows from 4/4.
