# Repairs proposed from iteration 5

The live batch remains frozen until completion. These proposals preserve adopted expectations, the same logical agents, and current retry budgets.

## Make undeclared response text an explicit review obligation

Pigeon and Douglass release source-dependent interpretations immediately after correctly declared quotations. The per-source missing-claims scan returns no omissions. With no canonical records, that inventory is empty even when a reply asserts an unsupported future event.

Replace the per-source audit with an application-computed inventory of uncovered response spans. Compute exact character intervals from the current reply after unioning occurrences of declared claim spans and exact quotations. Keep the original complete response available so fragments retain context. Every non-whitespace remainder receives an index and exact offsets/text. The reviewer must classify every index as presentation, reader reflection, or source-dependent text needing a current finding. Headers are presentation only when their factual attribution is already established by the declared source; questions and tentative interpretations are not categorically exempt. Claims and quotations removed from this view still receive their existing independent checks. Coverage is a review obligation, not deterministic proof that the model's classification is correct.

Remove the obsolete source audit/reference interface and migrate controlled callers and fixtures. Recompute derived facts after deserialization; a supplied or stale inventory cannot hide prose. Preserve existing claim audits, exact quote checks, finding binding, revision resolution, and release checks.

## Show joint claim membership as an application fact

Roses S2 already repeats its full joint interpretation in both contributing book declarations, yet review asks for exactly that repair and fails both mappings as though each must support the whole claim alone. Provide a computed claim-group projection: exact claim text plus every declaring declaration/claim index and source reference. This establishes membership only. Review must still judge each source's actual contribution, the full claim across its declared sources, and unsupported advice or causal claims within overbroad mappings. An available but undeclared source remains unusable for repairing that claim.

## Bind boundary proof to canonical text

Douglass's memory assessment attaches an unrelated but valid Chapter 3 record ID to an explanation of the Chapter 6 teaching prohibition. The correct teaching passage is available. A valid ID alone cannot establish that the judge read or correctly bound its content. Require short verbatim canonical support beside private proof IDs and validate that the quote belongs to the named record before granting scope. Source indices alone would not establish this binding. Exact matching still does not prove semantic relevance, which remains the Librarian's task. No private proof text enters the Muse handoff.

The boundary worker is reviewing the smallest compatible shape and scope before implementation. Avoid requiring every incidental reader clause or missing intermediate event as a prerequisite for a uniquely located final stop; that would add the false negatives the user asked us to avoid.

## Validation

Meaningful offline checks cover exact interval coverage, overlapping and repeated spans, stale supplied derived facts, complete audit indices and matching findings, complementary versus missing source contributions, and wrong-ID/right-quote boundary rejection. Existing personal/no-source and quotation-header controls must continue to work. A subsequent reserved live batch will test semantics; offline fixtures cannot establish model judgment quality. Preserve all failing artifacts and do not change adopted answers to obtain a pass.
