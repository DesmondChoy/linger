# Comparison after iteration 10

Batch10 completes six targeted suites with unchanged source hashes. Native checks pass5/9 Scenes, release11/12 cases and mapping3/4 controls; smoke fails. Five Provenance calls end with provider_error, no returned model output, no usage and null HTTP status: Roses S1/S2, Hume S1, release identity-theme and smoke. This differs from a completed semantic rejection. Quotation/mapping defects remain visible in several unreleased drafts, so provider recovery alone is not an all-pass fix.

## Restore the original reasoning level for a bounded comparison

Keep the same approved OpenAI Luna model, repaired personal-reflection policy, audit ordering, concise support summary and existing retries. Set its explicit reasoning effort to medium, the original default level used before the high-effort experiment. Keep the value in the central model configuration and batch metadata. The same mock Responses test must show that medium actually reaches the SDK request. Other models remain unchanged.

The high setting has not established an overall reliability benefit: source-free Pigeon reflection now credibly passes under the repaired policy, but incomplete collective support is still wrongly approved, and provider failures increased. Correlation does not establish that reasoning effort caused those failures. The next targeted comparison should separate model-setting behavior from another speculative runtime change.

Retain support_summary as a concise, fallible evidence conclusion: it exposes the exact wrong assertion that the earlier named record contains the later verse account. It does not independently verify support. Do not add semantic regular expressions or increase retries.

## Classify known provider failure kinds without contents

Add optional provider_error_kind with closed values http, timeout, connection, other to evaluation exchanges. Only classify an outer ModelAPIError. ModelHTTPError maps to http. Otherwise inspect its immediate preserved SDK cause: APITimeoutError before APIConnectionError, then APIStatusError; unmatched provider errors map to other. Non-provider exceptions remain null. Keep provider_status_code unchanged and do not copy messages, bodies, requests, model names or arbitrary exception class names. Known connection classification does not establish the physical network cause. Tests exercise actual installed SDK exception mapping and old artifact compatibility. No timeout, retry, endpoint or telemetry authority change.

## Defer evidence projection

The mapping summary wrongly says a declared early record contains a later verse detail. Unused available records and duplicated raw tool results may confuse source assignment, but familiar-book model knowledge could also supply it. Hiding all unused sources could weaken detection of undeclared memories or contrary evidence, while filtering only canonical arrays leaves duplicate raw results visible. Do not implement that projection before resolving the simpler setting/transport comparison. Preserve every current source and expected answer.
