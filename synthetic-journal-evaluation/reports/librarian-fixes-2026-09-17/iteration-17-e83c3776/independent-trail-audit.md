# Independent iteration17 trail audit

Audited while iteration18 was already reserved and running. No source changes, provider calls, hidden retries or additional reservations were made. This is a separate collaboration review of root's trail; I authored the mapping/smoke analysis and the Prove portion of repairs17/18, so those implementation claims are not independently authored by me.

Verified all103 entries in `report-index.json`: evaluation JSON, analysis JSON and rendered Markdown exist; every ordered Scene ID in each saved evaluation matches both the analysis facts and its completed review. This establishes complete recorded review coverage, not a new independent semantic reread of all103 historical runs.

Iteration17 before/after manifests match exactly for222 files. All24 Backstory/Ground-truth/adoption hashes in its eight native summaries match their current authoritative files. Recounted22/29 native Scenes and24/32 judgments. Direct has11 results from12 cases and11/12 correct strengths; release has10/12 strict successes, with `caucus-prizes` and `identity-theme` declining; mapping3/4 and smoke failure agree with root's summary. The full-set success gate remains false.

Confirmed provider429 failures in exactly five native Scenes: Hume S1, Pigeon reflection, Farm inference, Roses S1 and Pinocchio inference. Pinocchio contains two affected exchanges. This is distinct from component failures. Smoke's revision429 has no returned revised candidate; its retained draft history cannot be counted as new revision outputs. The direct failed case lacks the underlying agent transcript, so root correctly leaves its underlying cause unresolved rather than calling it another proven429.

Read root's iteration17 summary and the corresponding decision rows through iteration18 reservation. The rows preserve the source-projection tradeoff, qualified model improvement, actual semantic false negatives, provider interruptions and prospective concurrency reduction. The current ledger contains18/20 reservations, including running18; iteration17's17/20 statement is historical at its completion. The index's current local checkpoint correctly records1896 tests and670 subtests for repair18, then95 focused tests and10 subtests for a subsequent prompt-only clarification. Both supporting log files contain those results. These are not misrepresented as tests run by this audit.

No verified numerical overclaim or missing evidence pointer was found in the inspected completion/repair rows. Root keeps the pending Roses human expectation amendment unapplied and distinguishes mechanical pass counts from semantic issues. No commit/push gate is claimed met.

Attention: inspect the complete-joint mapping's relabeled but still incorrect `misattribution` rejection; the two misleading native personal-control passes; and the provider-interrupted paths whose repair outcome is unobserved. Source projection improves some samples but still requires literal-proof repairs and does not establish semantic completeness by itself. The accepted prompt growth, up to94,264 serialized characters in the measured native inputs, is a real cost. Runtime source growth and new concurrency are prospective experiments, not established remedies for429 or reviewer reliability. Remote Logfire visibility was not independently checked by this audit.
