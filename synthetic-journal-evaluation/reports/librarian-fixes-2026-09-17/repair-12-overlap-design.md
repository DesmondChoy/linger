# Overlapping declared claim coverage

Batch 12 smoke contains a web declaration for S + T and a book declaration for S. Exact-text grouping loses their joint membership for S. The book declaration must never gain support authority for T.

Preserve one group for each distinct original claim, in first-appearance order. Change `build_claim_support_groups(evidence_uses, response)` to derive exact response occurrences and intersections. Each group keeps its complete claim and an ordered `occurrences` table of absolute Python-character start/end positions. Each source reference keeps its original declaration/claim indices and source identity, adds `direct` (the original claim equals this group claim), and `coverage` entries containing group occurrence index, absolute start/end, and exact covered text. Every intersecting declaration is projected; no declaration expands beyond its exact response occurrences. Merge adjacent/overlapping intersections only within the same group occurrence and member. No semantic splitting, source inference, normalization, or truncation.

For S + T, web covers the whole claim and book covers S only. For S, both cover S. If S repeats outside the web's longer declaration, its occurrence has only book coverage. Membership in one occurrence grants no authority in another. The existing single group support judgment must assess all occurrences in their full response context; source contributions may only concern their listed coverage. A positive source excerpt remains bound to that named canonical source. The application projects scope; semantic entailment remains the reviewer's responsibility.

A projected overlap is an available contribution, not a new assertion by Muse. A negative contribution from an overlap-only member does not itself require a wrong-mapping finding: that source may validly support another part of its original larger claim. Every direct member still requires a finding when noncontributing, and every unsupported whole group still requires a finding on the actual full claim/direct mapping. This prevents projection from manufacturing invalid declarations while preserving all original wrong-source checks. The accepted-claim retention guard uses the same projection and requires positive direct contributions; optional overlap members do not create new preservation failures.

Implementation is limited to Provenance models and candidate-review instructions, the claim-retention helper caller, and focused tests. Derived projections remain recomputed and supplied serialized tables are discarded. Tests cover real smoke S/T, partial overlaps, separate repeated occurrences, negative T authority, forged tables, unrelated overlap contributions, retained claims, and existing wrong-source/excerpt gates. No additional calls, retries, verdict authority, or public quotation changes.

## Verification

The smoke nested-claim, partial-overlap, and repeated-occurrence tests failed against the prior projection (missing members/occurrences). All now pass. The final focused run of `tests/test_provenance*.py` plus `tests/test_muse_claim_retention.py` passed 226 tests and 39 subtests. One preexisting exact-membership assertion was migrated to distinguish direct declarations from newly visible overlap coverage while retaining distinct claim text. Inventory enforcement, named-source excerpt binding, mandatory direct-member and unsupported-group findings, forged-derived-table recomputation, and revision retention remain covered. `git diff --check` passes. No live calls were made.

The interval sweep merges declared coverage before intersecting it with monotonically ordered claim occurrences, avoiding an all-pairs occurrence scan. Audit group count remains the original distinct-claim count. Highly repeated text can still enlarge the exact occurrence/coverage input; no occurrences are silently discarded. This projection does not establish semantic support: model classification, contribution relevance, and whole-claim completeness still require independent live evaluation.
