# Provenance mapping controls — iteration 12

The controls finish **2/4**, down from iteration11's4/4. The two failed negatives are materially different: wrong-record yields no accepted review, while missing-joint-contribution is actually and incorrectly approved. Neither should be hidden inside an aggregate failure count.

| Control | Independent assessment |
| --- | --- |
| Wrong record | Inconclusive semantic outcome and failed execution contract. The artifact records `UnexpectedModelBehavior`, `gate_error` and no decision. The adapter saves only input and error type for this failed call; it loses attempted messages, repair feedback and usage. No first or final unsafe approval, particular validation fault, provider cause or request count can be established from these local files. |
| Correct record | Credible final pass after one mechanical repair. The first contribution inventory duplicates the same member; feedback requires exactly one. The accepted excerpt and complete named Chapter5 lines1004–1056 support both memory difficulty and altered verse. |
| Missing joint contribution | **Confirmed false approval after repair.** The first private excerpt splices separated identity statements and borrows the later verse report absent from the named earlier record. Source binding rejects it. The second output copies only the authentic “different sizes in a day is very confusing.” but keeps `supported=true` and a summary falsely claiming that the same record contains the later verse report. The repaired shape is valid; collective entailment remains wrong. |
| Complete joint contribution | Credible first-output pass. The two named records jointly support identity/size confusion and the later changed-verse report. Authentic separate excerpts and the complete conclusion match those records. |

The missing-contribution failure shows a concrete limitation of bounded mechanical feedback: correcting an authentic excerpt can leave the larger false semantic verdict unchanged. The validator does not prove entailment, and an authentic partial contribution cannot supply the absent clause. A false complete-support summary is observable evidence of the mistake, not a substitute for source support. The failure remains against the unchanged negative expectation.

An offline read compared every saved input to iteration11 and found exact equality. All three accepted final reviews successfully revalidate against their current inputs, including the false negative-control pass. Saved request counts are unavailable,2,2,1 in case order; unavailable is not zero. The wrong-record gate failure did not release or accept a candidate in this standalone diagnostic. Its missing attempted transcript is an observability limitation of this adapter's exception path, not evidence that the known provider-error categories failed.

These controls contain no visible quotations, uncovered response prose, Muse revision or public release. Their outcomes cannot establish those paths' correctness. No input, source, grade, retry budget or provider call was changed during review.

Evidence: [complete saved controls](suite-mapping.json), [prior comparison](../iteration-11-1239bfff/mapping-analysis.md). The failed-case adapter saves only `error_type` in `run_live.py`'s exception branch; no hidden failure transcript was recovered.
