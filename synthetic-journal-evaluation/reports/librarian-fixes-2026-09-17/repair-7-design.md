# Repair after iteration 7

Iteration 7 completed with unchanged source hashes. Four complete required suites pass (Pigeon, Hume, Keller, outside essay); the whole set does not. Keep all adopted expectations and failures. Farm's two content-filtered incomplete provider responses are an external execution block, not a completed inference or evidence-key disagreement.

## Review the full claim and each source separately

Roses rejects a valid two-record comparison as if every record must prove the whole sentence. Conversely the four fixed mapping controls approve both deliberately wrong mappings by treating an available, undeclared record as if it supported the declared one. Replace the flat per-declaration support boolean with one collective judgment per projected exact claim group plus a distinct contribution judgment for each named member. Each positive member supplies a concise source_excerpt bound to that exact source kind and ID. Private excerpts use the already-established whitespace-only normalization, preserving words, punctuation, case and markup. Public exact_quote checks remain character-strict.

One pure grouping helper supplies stable group indices to the computed input and revision preparation. Require every group and member once, a current finding for each negative result, and a canonical source-local excerpt for each positive contribution. A missing second source can yield contributes=true for the first source but supported=false for the whole claim. An irrelevant extra declaration still requires a finding even if other members suffice. Substring checks are proof of source identity, not semantic entailment.

## Audit quotation candidates independently of claim coverage

Alice's inference revision repairs the requested narrator text but still omits its quotation declaration. Roses quotes stored wording in an open question without binding it. A declared claim currently hides such text from uncovered-span classification. Project balanced straight/curly double-quoted interiors separately and require an explicit per-span classification. Source quotations need full occurrence-level binding to a current valid declaration or a current finding. Earlier reader wording retains the existing verified SessionLineUse.quote path. Titles, proposed wording and scare quotes remain semantic distinctions; coincidental source matches never classify a quote automatically.

This projection deliberately does not establish complete requested narrator text, single-quoted fragments, unbalanced quotations or blockquotes. Full semantic review remains necessary. It adds no model call or retry.

## Prevent repair from silently dropping accepted declarations

Douglass repairs one mapping but silently removes a different accepted mapping while leaving its exact text. Prepare revision obligations only from collectively supported groups with every contribution accepted and no response finding touching their text or source declarations. A global or unclear finding conservatively removes the obligation. Rejected/overbroad mappings, such as the Hume suggestion wrongly assigned to an essay, must remain free to lose their declarations.

For unchanged retained text, require coverage by the union of current exact declared spans; allow split and expanded mappings, ignore whitespace gaps, check repeated occurrences. Never copy source IDs automatically or carry forward a passing verdict. Fresh Provenance still judges all current assignments.

Carry first-review source-quote interiors as structural repair obligations too. If a retained interior is still quoted, require a complete current valid binding inside Muse's existing output-repair budget. Removing or paraphrasing it clears this structural obligation but does not prove that the reader's quotation request was fulfilled. The application supplies the same earlier reader Lines already in released conversation for checking session quotations; their content remains untrusted and grants no book authority.

Ownership: history_beads owns Provenance group model/central validation, skill and controlled fixture migration; grade_audit owns the standalone quotation-audit helper and tests; root owns typed Muse revision obligations, preparation and repair validation. runtime_evidence independently reviews integration and the decision trail. Existing tools, output retries, one response revision, release scope and storage authority remain unchanged.
