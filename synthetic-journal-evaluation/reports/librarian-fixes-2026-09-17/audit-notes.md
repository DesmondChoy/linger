# Independent decision-trail audit

Reviewer: the bounded `/root/grade_audit` collaboration agent, independent of the root writer of `decisions.tsv`. Reviewed the active task evidence, the decision trail through the live-3 launch checkpoint, saved menu, reservations, summaries, source manifests, native review coverage and relevant local-test logs. The audit did not edit the trail or run providers.

## Established checkpoints

- Iteration 1 was a full eleven-suite batch. Its summary records unchanged runtime sources and 17/29 native Scenes passing. All eight native analysis files contain reviews covering their complete Scene lists. Component and smoke reports are present. No complete-suite pass was claimed.
- Iteration 2 was an explicitly targeted six-suite batch, not a complete evaluation set. Its summary records unchanged sources, 11/17 native Scenes passing, and only the Hume connection suite and smoke passing overall. All five native analysis files now cover every Scene. The budget records both completed iterations as failed, including partial batches in the twenty-iteration allowance.
- The first cited complete local result is present in `/private/tmp/linger-full-tests-2.log` (1590 tests, 630 subtests). The later live-1 integration log contains 1594 tests and 633 subtests, matching the trail. The live-2 local log contains 1628 tests and 633 subtests.
- `local-tests-before-iteration-03.log` is an honestly retained failed attempt: five review-server tests could not bind localhost in the sandbox. `local-review-tests-iteration-03.log` records 31 passing review-server tests with binding permission. The subsequent `local-tests-integrated-iteration-03.log` now records **1656 tests and 633 subtests passed**. Do not cite the initial failed log alone as the final check.
- All four blocked legacy entries remain excluded from live runs. The schema-only menu-1 successor is separate and unadopted; menu 2 validates but has no adoption; menu 3/5 require human semantic choices. No original Ground truth changes were used to obtain passing results.

## Attention

1. **The complete eligible set is not every saved Librarian-related Scenario.** The eleven runnable suites exclude saved-menu 1/2/3/5. Preserve this distinction in completion claims; a future full eleven-suite pass does not establish that these four legacy Scenarios passed. See [legacy-readiness-review.md](legacy-readiness-review.md).
2. **Expectation decisions remain open.** The strict support-set failures and component/source permissions in [expectation-review.md](expectation-review.md) are not runtime authority failures by default. Conversely, they cannot be silently removed to count a suite as passing. Farm’s missing required intermediate anchor is different from accepting extra valid evidence. Maintain an explicit adopted decision before changing either.
3. **The repair-2 quotation remedy still needs live evidence.** Computed `quote_checks` provides trustworthy exact-string facts and the review instructions prohibit contradicting those facts. There is no separate deterministic rejection of a reviewer’s whitespace objection; findings remain model judgments. Local tests establish mechanics and fail-closed behavior, not that the live reviewer will always stop inventing a mismatch. Iteration 3 is needed for that behavioral conclusion.
4. **Some evidence pointers are less durable than the rest.** The early local-validation row references a temporary-file log. The completed live-1 row says “native Scene analysis reports” rather than naming an index; its summary’s native analysis paths do resolve. Before durable handoff, append a row pointing to preserved logs and the exact summary or report index rather than relying on temporary context. Do not rewrite historical rows.
5. **Focused-test result rows cite test files rather than output logs.** Those files establish coverage but not that the quoted counts ran. Counts are supported by the active task’s tool results and now by the integrated 1656-test log, which the live-3 launch row cites as a durable checkpoint. Historical failed logs such as `local-tests-final-before-live.log` must remain labeled as superseded attempts.
6. **Remote telemetry visibility remains unverified.** Native reports consistently distinguish an exported URL/flush from independent remote read-back. Keep that qualification; do not convert exported telemetry into a claim that every remote trace was inspected.

No fabricated pass, hidden adoption, or unrecorded live retry was found in the inspected records. The source-hash-before/after summaries support the runtime freeze claims for iterations 1 and 2. Live iteration 3 is a new in-progress checkpoint and was not graded by this audit.


## Completed checkpoint through iteration 4

This addendum supersedes the earlier in-progress status; it does not rewrite that historical audit. The decision trail now records completed iterations 3 and 4 and explicitly makes no all-pass or commit/push claim. The audit re-read both batch summaries, reservations, budget, source manifests, all eleven new native review objects, current original-source hashes, the updated report index, the local-validation log and the release-component findings. Detailed semantic review by this agent covers iteration-3 Pigeon, Douglass, Alice, Keller and Pinocchio, iteration-4 Keller, and the separate Farm proof investigation. Other native semantic findings here are attributed to their saved reviewer reports.

| Batch | Coverage | Native Scene checks | Suite passes | Source freeze |
| --- | --- | --- | --- | --- |
| 3 | All eleven eligible suites | 21/29 | Douglass and smoke; 2/11 overall | Before and after manifests exactly equal |
| 4 | Targeted Roses, Hume, Keller and release | 8/10 | Hume and Keller; 2/4 overall | Before and after manifests exactly equal |

The budget contains four completed reservations, all with `all_pass=false`; targeted batches count toward the twenty-batch allowance. The native analysis coverage matches each exact Scene list in order: eight reports for iteration 3 and three for iteration 4. Every original Backstory, Ground truth and adoption hash checked against those native summaries still matches. The [report index](report-index.json) now resolves all 24 native reports from iterations 1–4. Component and smoke analyses are separate from those 24 reports. No unrecorded live retry, altered adoption or fabricated pass was found in this audit scope.

The latest completed local checkpoint is [local-tests-before-iteration-04.log](local-tests-before-iteration-04.log): **1664 tests and 633 subtests passed**. The previously temporary early result is now preserved in [local-tests-early-integration.log](local-tests-early-integration.log), with 1590 tests and 630 subtests. The trail appended a durable index/log checkpoint instead of rewriting earlier rows; this resolves the temporary-evidence-pointer concern above.

### Conclusions that the records support

- Boundary repair now reaches real positive answers for Pigeon, Alice and Pinocchio. Their iteration-3 failed support checks reject extra valid records despite coverage of their adopted anchors. Farm is different: it omits an adopted recognition anchor as well as adding another valid record. The exact offline matcher probe in [farm-proof-review.md](farm-proof-review.md) establishes both independent mismatches. It does not demonstrate a wrong chapter grant.
- The earlier quotation-mechanics uncertainty has narrower positive live evidence: exact requested quotes are present in the reviewed iteration-3 book answers, and iteration-4 Keller retains its exact quotation through a successful header revision. This establishes observed success, not a guarantee that a semantic reviewer can never invent another quotation objection. In Keller’s inferred Scene, the remaining review was about chapter-wide finality rather than whitespace or an unmapped header.
- Keller’s ambiguous pet Scene explicitly compares the canary and crab and grants nothing; Douglass’s ambiguous return similarly compares distinct occurrences. These are genuine uncertainty judgments, not passing clarification labels caused by execution errors. The generic clarification question remains a helpfulness limitation in the native reviews.
- Iteration-4 release produces all twelve Muse replies with final Provenance approval, but still fails the frozen identity key and native citation precision (0.8333 versus 0.95). Its eleven mechanically passing cases must not be described as twelve passes. Nor may iteration-3 native aggregate target success override that batch’s stricter individual-case failures.

### Open gaps at this checkpoint

1. **Human expectation decisions are still required.** No permission to relax support sets, component calibration or source allowlists has been recorded in the inspected artifacts. Farm’s missing bridge is not covered by a decision to allow extra evidence. The current runtime contract accepts independently grounded earlier knowledge plus an identified final stop; adding all intermediate events solely to satisfy the adopted key would introduce a new requirement and false-negative risk. Keep the existing failure until its intended requirement is resolved.
2. **Semantic failures remain separate from fixed-key failures.** The iteration-4 Roses report finds a missing expected quotation in S1 and an unsupported possible personal cause in S2. The saved release reply for `future-cheshire` presupposes an upcoming passage with “when you reach it” and overstates the evidence search as proving absence from the entire eligible text. Its zero returned-evidence spoiler metric cannot detect those claims. A corrected evidence key alone would not establish a clean complete-suite result.
3. **The final eligible-set gate has not passed.** Iteration 4 is a targeted subset and cannot replace a final unchanged-source full eleven-suite verification. Earlier passing outputs on other revisions should remain visible as progress, not combined into an all-pass result. The four blocked legacy menu entries remain outside these eligible eleven, as documented in [legacy-readiness-review.md](legacy-readiness-review.md).
4. **Remote telemetry visibility remains unverified.** Saved trace URLs and export flushes are useful navigation records, not independent remote read-back evidence.

Evidence: [iteration-3 summary](iteration-03-4715c97d/summary.json), [iteration-4 summary](iteration-04-797d2eda/summary.json), [iteration-4 component analysis](iteration-04-797d2eda/component-analysis.md), [live budget](live-budget.json), [decision trail](decisions.tsv), [expectation decisions](expectation-review.md). Runtime fixes and subsequent iterations after this completed checkpoint are outside this addendum; append later evidence separately.

## Completed checkpoint through iteration 6

This addendum was reviewed by `/root/runtime_evidence`, independently of the root decision-trail writer. It appends to the earlier `/root/grade_audit` checkpoints. I inspected both new batch summaries, before/after source manifests, all sixteen native review objects for complete Scene coverage, original input hashes, the diagnostic log, budget, and decision-trail completion rows. My detailed semantic review covers all Alice, Keller, and Pinocchio Scenes in batches 5 and 6, plus batch-5 component and smoke reports; other semantic conclusions are attributed to their saved reports.

| Batch | Coverage | Native Scene checks | Overall passing suites | Source freeze |
| --- | --- | --- | --- | --- |
| 5 | All eleven eligible suites | 22/29 | Pigeon, Keller, smoke: 3/11 | All 218 before/after manifest entries equal |
| 6 | All eleven eligible suites plus mapping diagnostic | 21/29 | Hume, smoke: 2/12 selected | All 218 before/after manifest entries equal |

Every native review lists its exact Scene IDs in order. Current Backstory, Ground truth, and adoption hashes match the originals recorded by all sixteen native summaries. Recorded grades were not rewritten to match reviewer judgments. The budget at this completed checkpoint has six reservations, all complete with `all_pass=false`; subsets and the failed diagnostic remain counted. The full eleven-suite gate has not passed, and the four blocked legacy menu entries remain outside that eligible set. Historical results from different source revisions cannot be combined into a current all-pass claim.

The iteration-6 mapping diagnostic is **ungraded**, not a semantic pass or a semantic model failure. Its first model call reached result handling, then ordinary JSON serialization rejected a Decimal usage cost. The cleanup attempted to serialize the same object again and also failed. The persisted progress file has an active first case, `status=running`, and zero saved cases; no final `suite-mapping.json` exists. The batch status faithfully records a nonzero exit and report error. Do not infer that zero saved cases means no provider request occurred, or infer the first model's semantic decision from the missing report. See [diagnostic log](iteration-06-0b927fa3/suite-mapping.log), [progress](iteration-06-0b927fa3/suite-mapping.progress.json), and [status](iteration-06-0b927fa3/suite-mapping.status.json). No retry of that failed run was made in this review.

The new native analyses distinguish model output construction from expectation disputes. Alice's failed inferred boundary excerpts preserve canonical words but flatten line breaks, including in the attempted repair. Keller's failed inferred excerpt also substitutes “its” for canonical “his”; that is not a whitespace-only mismatch. Both fail closed to clarification before response retrieval. Pinocchio's inferred Scene grants Chapter 9 with canonical prior and stopping-event evidence, releases its exact quote, and fails only the frozen additional-support rule. The ambiguous Scenes actually compare repeated occurrences, including the previously missed Keller canary and Pinocchio Chapter 26 alternative, without releasing their plots. The explicit Alice answer retains an unbound quoted source fragment; Pinocchio's revised closing question retains a personal causal presupposition. These limitations are recorded separately from their mechanical passes.

Expectation decisions in [expectation-review.md](expectation-review.md) remain open: additional verified support, direct returned-record permissions, component identity-theme calibration/ranges, and Farm's missing intermediate recognition anchor. The last question is independent of accepting extra evidence. Nothing in the inspected records adopts the proposed replacements or authorizes gold changes. A correct scope or a plausible thematic answer alone does not erase a frozen failure, and a changed key alone would not repair a flawed released response.

I separately reviewed repair-6 source changes after the batch finished. No verified gate weakening was found. Private `source_excerpt` binding collapses whitespace runs only, preserving lexical text, record identity, inventory and grant checks; visible `exact_quote` validation is unchanged. Prompt clarifications preserve current-Line authority for intense distress, use normal revision for false emotional refusal, require all source quotations and negative source claims to be mapped, keep research background distinct from measured findings, and prevent causal presuppositions in closing questions. The mapping adapter uses `TypeAdapter(RunUsage)` in JSON mode for Decimal serialization. It adds no retry or budget reset. Independent offline verification passed **53 tests and 8 subtests** across boundary source binding/inventory, Provenance skill, diagnostic adapter, and driver checks. These establish mechanics and prompt presence; live semantic improvement remains unproven at this checkpoint.

Evidence: [batch 5](iteration-05-8f52910d/summary.json), [batch 6](iteration-06-0b927fa3/summary.json), [decision trail](decisions.tsv), [budget](live-budget.json). Native summaries point to the individual rendered analyses. Telemetry URLs and successful flushes still do not establish independent remote trace visibility. No fabricated all-pass, altered adoption, hidden retry, or budget reset was found in this audit scope.

## Completed checkpoint through iteration 7

Reviewer `/root/runtime_evidence` checked the new full-set summary, source manifests, all eight new native analyses, and the full inventory of **48 native reports across iterations 1–7**. Every report contains exactly its recorded Scene list in order and has a rendered Markdown companion. Every current Backstory, Ground truth, and adoption hash still matches the native run's input manifest. Both iteration-7 source manifests are equal, supporting the recorded source freeze.

Iteration 7 ran all eleven eligible suites plus the four-case mapping diagnostic. Native Scene checks passed **22/29**. Pigeon, Hume, Keller, and smoke passed as suites; the complete selected set did not pass. The failed run remains one counted reservation; no evidence or adoption was changed to turn it green.

The mapping diagnostic now completed after the serialization correction and records actual semantic outcomes: both positive controls passed and **both negative controls were wrongly approved**. All four reviews returned `pass` with every declared claim marked supported. One negative assigns the memory/changed-verse claim solely to the earlier identity/size record; the other maps a joint claim only to that earlier record, omitting the verse contribution. Current structural validation accepts those reviews. These are confirmed source-support false approvals, not risk-code disagreement or missing serialization. Borrowing facts from an available but undeclared source is a plausible mechanism, not known private reasoning. The saved [mapping analysis](iteration-07-e1e6fe49/mapping-analysis.md) separates that inference from the observed defect. Negative controls remain failed; the diagnostic is not part of a fabricated all-pass claim.

Farm's inferred Scene has a different failure. Both saved model responses report `finish_reason=content_filter` and end partway through the first private source excerpt, after “giving birth between them” and “giving birth between them to”. The application never receives a complete candidate, cannot validate or grant its scope, and releases a generic clarification. The first incomplete JSON triggers the already bounded repair; the second is again incomplete. No conclusion about semantic proof quality or the strict gold support set can be drawn from this partial output. The earlier false emotional pause is absent: preflight permits reflection and Muse actually routes. The record does not identify why the provider filtered the text. No prompt or input bypass was attempted in this audit.

Detailed reviews of Alice, Keller, and Pinocchio establish that whitespace-tolerant private source excerpts can now support correct scope grants while preserving canonical words. Keller releases both full quotations and passes every Scene. Alice's inferred Scene reaches Chapter 3 and bounded retrieval but declines after a distinct second quotation remains undeclared through revision; this is a real repair failure in addition to the pending extra-support expectation. Pinocchio reaches Chapter 9, releases its requested quotation, and fails only the strict support-set key. Both negative ambiguity cases compare actual alternatives, and Keller's canary remains visible to the private decision. Mechanical passes still do not establish every prose detail or every intermediate reviewer judgment as correct.

All expectation questions previously listed remain pending, including component identity calibration, allowed additional evidence, and Farm's independent missing-intermediate-anchor question. A failed provider completion does not resolve those decisions. The last full eleven-suite gate is still failed; earlier successes on different revisions are not combined. This addendum covers completed iteration-7 evidence, not the later repair-7 design or its eventual validation. Remote trace visibility remains unverified.

Evidence: [iteration-7 summary](iteration-07-e1e6fe49/summary.json), [mapping diagnostic](iteration-07-e1e6fe49/suite-mapping.json), [Farm native summary](iteration-07-e1e6fe49/suite-7.native-summary.json), [budget](live-budget.json), and [expectation review](expectation-review.md). Native summaries link the full recorder artifacts and every rendered Scene analysis.

### Independent repair-7 integration review

After the iteration-7 freeze ended, `/root/runtime_evidence` reviewed the integrated grouped Provenance audit, private contribution excerpts, independent quotation audit, application-derived Muse revision obligations, and their actual orchestration/output-validator callers. No verified authority expansion, unintended gate weakening, or maintainability blocker was found in this scope. The focused local command covering `test_provenance_group_audit.py`, `test_provenance_quotation_audit.py`, `test_provenance_quote_checks.py`, `test_muse_claim_retention.py`, and `test_revision_review.py` completed with **79 tests passed**; it used offline settings and made no provider calls.

Group judgments explicitly separate collective support from individual contributions. Every group and every declared member is required once, positive contribution excerpts bind to the named canonical source kind and ID with whitespace-only normalization, and unsupported groups or noncontributing members require current response findings. An available undeclared source cannot mechanically satisfy a different source's excerpt. The computed group and quotation inventories discard supplied serialized projections and recompute from the current candidate.

The quotation audit is independent of claim coverage and requires every balanced curly/ASCII double-quote occurrence, including repeats and quotes inside mapped claims. A model-classified source quote must be wholly covered at that occurrence by a canonically verified declaration or produce an appropriate current finding. It never autoassigns a matching source. Current-reader classification additionally requires the exact words in the current Line; titles, suggested wording, and scare quotes remain semantic distinctions. Single or unbalanced quotation marks and narrator text outside pairs remain under whole-response semantic review, explicitly rather than being described as mechanically covered.

Revision preservation is a repair constraint, not inherited release approval. The application selects only unanimously accepted prior claim groups unaffected by a response or contributing-declaration finding. Rejected overbroad advice mappings remain removable. Exact surviving occurrences require coverage by the union of current declared spans, allowing split clauses and larger spans while ignoring only whitespace gaps. The helper does not retain source IDs or prove support. Previously classified source-quote interiors that remain quoted require current canonical binding. Rewritten claims and newly introduced quotations still require independent fresh review; the exact-retention check does not claim to recognize semantic paraphrases.

The implementation keeps the existing roles, single reviewed revision, bounded output repair, canonical evidence contexts, and final release validation. Authentic but irrelevant excerpts can still accompany a mistaken semantic verdict, and incorrect semantic quote classification remains possible; these are limitations requiring the unchanged live negative controls, not properties the local tests establish. No provider retries, model changes, budget reset, or expectation edits were introduced by this review.

## Completed checkpoint through iteration 8

Reviewer `/root/runtime_evidence` checked the completed batch summary, both source manifests, budget, all **56 native reports through iteration 8**, and original input/adoption hashes. Every native report contains its exact Scene list in order and a rendered Markdown companion. All recorded input/adoption hashes still match current files. The 220 source-manifest entries are identical before and after batch 8; later authorized repairs are separate from this frozen result.

The prior automatic approval denials consumed no reservation. Root records receiving explicit transfer permission before launching this run, and the budget now contains **eight** completed reservations. The earlier approval-block account remains historical evidence, not the current permission state. Iteration 8 ran the complete eligible eleven suites plus mapping controls and records `all_pass=false`. Native checks passed **20/29** Scenes. Douglass, Keller, and mapping passed as suites; no final complete-set success is claimed. Frozen expectations and adoption decisions remain unchanged.

Detailed independent reading here covers all Farm, Keller, and Pinocchio Scenes. Farm's explicit answer now fails because Muse changes a correct canonical source while repairing extra quotation declarations: the final source lacks the retained loyalty/obedience and debate-ending claim. The new group audit correctly refuses to borrow from the available but undeclared longer record. Its inferred Scene is again content-filtered twice mid-excerpt; no complete decision exists, so this is not evidence that a disputed support key or semantic scope is wrong. Keller's four final answers are credible passes, including both complete requested quotations and actual canary/crab alternative comparison. Pinocchio's inferred output completes normally but copies a supplied memory ID incorrectly on both attempts. An offline probe shows the second candidate passes inventory, exact reader spans, and source-excerpt checks, then fails memory identity. Its first feedback omitted that concurrent error while reporting other structural faults. This is a separate actionable repair-feedback defect, not a content filter, gold dispute, or unsafe grant.

The saved mapping diagnostic records **4/4 correct accepted outcomes**, including both negative controls. This is not four correct first attempts: the wrong-record first output falsely approves by copying excerpts from the undeclared later source, and the source-local validator catches that borrowing plus duplicated member inventory. Its bounded repair returns the correct revise decision. The correct-record control also needs one inventory repair; both joint controls finish in one request. The component report's appropriately limited conclusion is that these four controls now behave correctly, not that authentic excerpts mechanically prove all semantic entailment.

Root's [component analysis](iteration-08-a0b8741f/component-analysis.md) records direct **11/12** and release **10/12** strict passes, with release aggregate targets also false. Root's [smoke analysis](iteration-08-a0b8741f/smoke8-analysis.md) documents a final reviewer false rejection despite a repaired, fully bound quotation. These findings are attributed to those saved full-transcript reviews; this audit does not claim a second independent semantic review of every component and smoke reply. Neither the mapping diagnostic's success nor a safe fallback erases these application failures.

I subsequently implemented the separately authorized Pinocchio structural-feedback fix in the raw candidate validator and focused test file. That implementation is disclosed here, not presented as an independent review of my own code. Five new regressions failed before the fix, and 67 focused boundary tests passed after. The saved first malformed candidate now reports its memory-ID errors alongside all existing inventory faults. The unchanged single repair, typed after-validation, application recheck, and mixed-memory semantics are preserved. These are offline results only; they do not alter iteration-8 artifacts or establish success of the next live configuration.

Attention remains on the same unresolved expectation/adoption decisions, the failed full-set gate, and provider-filtered incomplete Farm outputs. The four legacy excluded Scenarios are still outside the eligible eleven. No expectation mutation, hidden replay, budget reset, or combined cross-revision all-pass claim was found in this audit scope. Remote telemetry read-back remains unverified. Evidence: [batch summary](iteration-08-a0b8741f/summary.json), [mapping analysis](iteration-08-a0b8741f/mapping-analysis.md), [budget](live-budget.json), and [repair design](repair-8-design.md).

## Completed checkpoint through iteration 9

Reviewer `/root/runtime_evidence` checked the completed targeted summary, decision row, source manifests, budget, and all **60 native reports through iteration 9**. Every review lists exactly its recorded Scenes in order and has a rendered Markdown companion. All original Backstory, Ground truth, and adoption hashes still match. Both iteration-9 source manifests contain the same 208 entries with equal hashes. Subsequent authorized repairs are separate from this frozen result.

Iteration 9 is a **targeted seven-suite experiment**, not the complete eleven-suite gate. Its explicit high-reasoning setting uses the same confirmed model. Native checks pass **7/13** Scenes, release passes **11/12** strict cases, and mapping passes **3/4** controls. No selected suite passes completely; `all_pass=false`. Nine of twenty reservations are counted. Unrun suites, including the direct notebook helper, cannot inherit passes from earlier revisions. Higher effort is not established as a reliability improvement.

I independently reviewed every Pinocchio Scene and its saved boundary/review exchanges. The inferred Scene correctly binds its supplied memory, grants Chapter 9, performs bounded retrieval, and releases the requested complete canonical quotation. Only the unchanged strict additional-support key fails. Both boundary attempts already use the correct memory ID; the first instead needs nesting/inventory feedback. Thus this run does not directly exercise or prove the necessity of repair-8's concurrent memory-ID feedback. The ambiguous Scene compares the actual Chapter 9 and Chapter 26 outings and withholds a grant. The explicit quotation and source-free personal answer pass, with the recorded prose-completeness limitation preserved.

The other native reviews remain attributed to their authors' full-transcript inspections. Pigeon's personal-only Scene still refuses useful reflection after inconsistent causal-claim guidance. Roses has both a provider-blocked Scene and a separately released comparison with stronger causal/punishment claims than the declared sources establish. Its closed extra-source key does not account for that semantic defect. Hume's final refusal in one Scene follows a provider failure, while its other comparison now releases a restrained, correctly quoted answer. These are distinct outcomes; blanket gold-mismatch or blanket semantic-failure labels would be misleading.

The [component analysis](iteration-09-d5215f1b/component-analysis.md) distinguishes the release aggregate's `targets_pass=true` from the failed strict identity case. Unlike iteration 8's bounded interpretation, the actual identity-theme answer again reverses the supported causal direction: size changes confuse Alice, while the answer says identity uncertainty makes new sizes harder to understand. Approving alternative gold evidence alone would not repair this accepted overclaim. The [mapping analysis](iteration-09-d5215f1b/mapping-analysis.md) documents a real false approval of the missing-joint-contribution negative: an authentic excerpt supports only one clause, but the whole compound group is marked supported. All four controls use one request and the candidate/source inputs match iteration 8. Source binding is functioning; semantic collective support remains wrong in this case.

Three saved high-reasoning Provenance failures—Roses, Hume, and smoke—are categorized only as `provider_error`. They do not expose a status or cause, so neither content filtering nor a particular transport failure is established. The [smoke analysis](iteration-09-d5215f1b/smoke9-analysis.md) also identifies an independently checkable defect in the unreleased draft: the full visible Descartes quotation ends with a comma, whereas its declaration omits that punctuation and the canonical text continues with a semicolon. The shorter declaration passes its substring check, but the complete occurrence fails quotation binding. Provider recovery alone therefore cannot make the saved draft releasable. Stage grades following the failed review are not evidence that earlier retrieval or selection did not execute.

The separately authorized diagnostic change now records only a known `ModelHTTPError` status integer in 400–599 in future evaluation exchanges, defaulting to null for other errors and historical artifacts. It cannot reconstruct these three past failures. I implemented that narrow change and therefore do not present this paragraph as an independent code review: the missing-field regressions failed before it, and 40 focused tests plus 11 subtests passed afterward. No raw exception text, provider body, model name, operational telemetry attribute, retry, or provider call was added.

The latest decision row accurately preserves the failed overall gate and no-commit state. Pending human expectation decisions remain pending; no adoption or source allowlist was changed in this audit. No hidden retry, budget reset, or fabricated all-pass was found. Remote Logfire visibility remains unverified.

## Completed checkpoint through iteration 10

Reviewer `/root/runtime_evidence` checked the completed targeted summary, latest decision rows, source manifests and all **63 native reports through iteration 10**. All reviews contain exactly the recorded Scenes in order, all rendered companions exist, and all original Backstory, Ground truth and adoption hashes still match. The 205 source entries are identical before and after batch10. Ten reservations are counted; later authorized diagnostic edits are distinct from the frozen run.

The preceding repair9 verification row is supported by saved logs:1750 tests plus633 subtests in the main collection and12 external-path checks plus8 subtests, totaling1762 tests and641 subtests. The row does not claim live semantic success. Independently reported scoped review results are additional review evidence, not added again to the complete-collection count.

Batch10 runs six selected suites under the same Luna model and explicit high reasoning, **not** the final full eleven-suite gate. Native checks pass **5/9** Scenes, release passes **11/12** strict cases and mapping passes **3/4** controls. Every selected suite still fails at least one check; `all_pass=false`. The improved Pigeon personal reflection is a local success, not complete task closure. Unrun suites cannot inherit earlier passes.

I independently inspected the smoke's saved exchanges, canonical sources, tool calls, public candidate and final failure. Its first four stages execute successfully; its sole review returns `provider_error`, null HTTP status and no response/output/usage. The eventual generic decline does not erase successful earlier execution. Two unreleased draft defects remain: the extra “Keep your temper.” quotation is undeclared and changes canonical punctuation, and the final Socrates/Caterpillar comparison maps its external philosophical-examination clause only to book evidence. The real offline occurrence-binding check rejects the first defect. Source selection and provider recovery alone would not make this draft releasable. The [smoke analysis](iteration-10-694474e9/smoke10-analysis.md) preserves both findings and the SDK investigation.

There are **five** incomplete provider-error reviews across the batch: Roses S1 and S2, Hume S1, release identity-theme, and smoke. All have null HTTP status and no returned review output. The [component analysis](iteration-10-694474e9/component-analysis.md) distinguishes identity's unreviewed reciprocal-causation draft from iteration9's completed false approval, and preserves its strict strength/range failures. Release aggregate `targets_pass=true` is weaker than the failed individual contract. Aggregate usage is unavailable after a failed review, not zero consumption. Root's all-case component review is attributed rather than represented here as a second independent review of every case.

The [mapping analysis](iteration-10-694474e9/mapping-analysis.md) records a persistent actual false approval. The missing-contribution negative supplies authentic identity evidence, but its new support summary falsely attributes the later verse report to that earlier named record. Its partial excerpt and audit shape pass correctly; the semantic whole-claim conclusion is wrong. The wrong-record negative initially borrows a later excerpt, is rejected by canonical source binding, and repairs to revise. Correct-record and complete-joint positives pass. The report properly describes narrower source projection as a hypothesis rather than a proven cause or authorized authority change; no such projection or gold relaxation occurred in this audit.

A read-only SDK probe confirms that the installed adapter preserves timeout/connection/status SDK exceptions as the immediate explicit cause of `ModelAPIError`; the current HTTP-only field cannot distinguish the first two. The SDK defaults and root's constructed-client inspection do not establish why any historical run failed. Root subsequently authorized only a closed future diagnostic category and separately planned a same-model medium-reasoning comparison. I implemented the evaluation-only category (`http`, `timeout`, `connection`, `other`) using known outer and immediate cause types, and disclose that as my implementation rather than independent self-review. Seven new cause cases failed before it;41 focused tests and18 subtests passed afterward. No raw exception/model/body, arbitrary-class export, deep cause walk, retry change, or provider call was added. Past null records remain unknown.

The completed decision row accurately records failure and no commit. Pending human expectation decisions remain pending, and no source allowlist or adoption changed. No hidden retry, budget reset or fabricated complete-set success was found. Remote telemetry visibility remains unverified.

## Completed checkpoint through iteration 11

Reviewer `/root/runtime_evidence` checked the completed medium-reasoning summary, latest decision row, report index and all **65 native reports through iteration11**. Every review contains its exact recorded Scene list in order and has a rendered companion. All original Backstory, Ground truth and adoption hashes still match. Both frozen source manifests contain the same202 entries with identical hashes. Eleven reservations are counted, with no hidden rerun or budget reset found.

This is a **targeted five-suite comparison**, not the full eleven-suite gate. Native checks pass **3/6** Scenes (3/8 judgments), release passes **9/12** strict cases, and mapping passes **4/4** controls. Smoke and mapping pass as suites; the other three selected suites fail. `all_pass=false` remains correct. The completed row and independently closed, previously verified issues do not misrepresent the overall evaluation task as complete. Unrun native books and the direct notebook helper remain untested on this particular frozen revision.

I independently inspected the complete smoke11 transcript, final source records, quotation occurrences, grouped mappings and release event. The final answer is a credible pass: it supplies a useful, qualified Self-Knowledge comparison, binds both complete book quotations, and maps the full joint interpretation to the book and selected web source. It distinguishes its suggested lens from an intended Caterpillar theory or a cause of Alice's confusion. The source's distinction between mental-state knowledge and knowledge of a persisting self limits the philosophical analogy; the answer does not establish a complete identity theory. Initial coverage findings led to a substantive mapping repair. The final review also needed two mechanical private-excerpt punctuation repairs before passing, so this success does not imply flawless first-attempt behavior. The saved final review and both quotations pass the real offline validators. See the [independent smoke review](iteration-11-1239bfff/smoke11-analysis.md).

The other full-transcript reviews are attributed to their authors. Hume S1 now releases a coherent comparison but omits the useful remembered comfort detail; S2 is falsely rejected for its ordinary no-proof opening despite an accepted joint synthesis. Roses retains actual missing mappings and a stronger personal-cause conclusion than its sources establish, alongside unnecessary objections to framing and a standalone No. Those genuine remaining defects and omitted adopted quotation requirements must not be erased by fixing the overstrict findings. Source-free controls remain useful.

Root's [release analysis](iteration-11-1239bfff/component-analysis.md) distinguishes the genuine giant-puppy failure—visible terminal comma outside the declaration—from the pigeon-serpent false rejection, whose two complete quotations already bind. It preserves identity-theme's unresolved strength/range and requested-interpretation distinctions. Its offline binding probe establishes exact copying, not entailment. It also identifies a concrete harness limitation: the component caller gives Provenance an empty current Line although Muse receives the actual case request. Thus existing source/quote checks remain meaningful, but these component runs do not faithfully test independent review of the reader's exact request. This new limitation is disclosed rather than retroactively changing the recorded grades.

The [mapping review](iteration-11-1239bfff/mapping-analysis.md) records4/4 correct final decisions with exact inputs equal to iteration10. The missing-contribution negative now correctly separates authentic partial support from incomplete collective support. The wrong-record negative still begins with borrowed proof and an unsafe approval; canonical source binding catches it and the bounded repair returns revise. All final controls validate and independently match their source records. This narrow success does not settle unrelated coverage, quotation or full-conversation failures.

No saved review in this batch reports a provider failure. That means the new closed provider-error categories are not exercised, not that the earlier unknown failures were diagnosed or that medium reasoning is proven to prevent them. Recorded release usage is available this time; its latency and token totals do not establish a causal explanation for the earlier cluster. No provider, external-search, source, expectation or adoption changes occurred during this audit. All human expectation questions remain pending, the full-set gate remains unmet, and remote telemetry visibility remains unverified.

### Independent repair-11 review

Reviewer `/root/runtime_evidence` inspected the scoped retained-quotation change, its actual Muse output-validator call, the component reader-message handoff and the new regressions against the saved repair11 design. No verified blocker, source-authority expansion or unintended acceptance was found. **75 focused tests passed** across `test_muse_claim_retention.py`, `test_muse_quote_repair.py`, `test_librarian_evaluation_integrity.py` and `test_provenance_quotation_audit.py`; this review made no provider calls or source edits.

`retained_source_quote` normalizes only outer whitespace and terminal sentence punctuation to determine whether a previously reviewed source-quote obligation survives. It requires a nonempty matching core and preserves words, case, internal whitespace, markup and other punctuation. It does not normalize the source check, assign evidence, rewrite the reply or approve a quotation. The current declaration must still match its authorized canonical record exactly and cover the complete current occurrence. The regression rejects a revised quotation whose new terminal comma remains outside its declaration, then accepts the complete canonical version. Changed wording, unquoted prose and an embedded different proposed formulation remain subject to ordinary independent review rather than newly classified by this helper. False reviewer claims about already valid quotations are outside this fix.

The release harness now passes the existing `request.message` unchanged as `capture_source_text`. Its FunctionModel regression exercises the actual draft-to-review orchestration and verifies that Provenance's current Line equals Muse's reader message, including both the case query and completed-chapter statement. It changes no expected case answer, source ceiling, release rule or retry count. Historical component reports retain the disclosed empty-Line limitation; this fix improves future evaluations without rewriting them. The live effect remains unverified at this checkpoint.

## Completed checkpoint through iteration 12

Reviewer `/root/runtime_evidence` checked the full batch summary and all **73 native reports through iteration12**. Every report has exactly the recorded Scene list in order and a rendered companion. All original Backstory, Ground truth and adoption hashes still match. Both frozen manifests contain the same220 source entries with identical hashes. Twelve of twenty reservations are counted. This batch executes all eleven core suites plus the four-case mapping diagnostic; it cannot be combined with earlier selected successes to claim all-pass.

The recorded results are **19/29 native Scenes,10/12 release cases,11/12 direct helper cases and2/4 mapping controls**, with smoke failed. No selected suite passes all its checks; `complete_evaluation_set=true` and `all_pass=false` are both accurate. I appended the completed iteration12 decision row through the repository log helper and added notes to `linger-9oqp` and `linger-55r9`, keeping both unresolved. Those bookkeeping mutations are disclosed rather than presented as another reviewer's independent work. Root retains the commit/push gate; no commit, push or live call was made here.

I independently reviewed all direct results, both mapping controls' actual failure modes, and the smoke's complete exchanges. Direct retrieval remains canonical and in scope, but its passed giant-puppy rationale invents throwing the stick where the source says holding it out. Two rationales also render source quotations nonliterally while their returned canonical text remains correct. These are weaknesses outside the helper's range/strength metrics, not a claim that Muse released those rationales. Identity-theme's strict mismatch remains distinct from its insufficiently qualified reciprocal interpretation. The adapter runs notebook case helpers, not the entire notebook, and does not save complete planner/strength-judge conversations. See [direct analysis](iteration-12-7098e696/componentdirect-analysis.md).

The mapping score of2/4 does **not** mean two accepted false approvals. Wrong-record records `UnexpectedModelBehavior` and gate_error with no accepted output; the adapter loses its attempted messages and usage, so the semantic attempts and specific failing validator remain unknown. Missing-joint-contribution is a confirmed false approval: a borrowed/spliced excerpt is rejected, then an authentic partial excerpt is accepted while the false whole-claim summary remains. The earlier named record contains no verse report. Correct-record and complete-joint positives pass, with one inventory repair on the former. All saved inputs equal iteration11; all three accepted reviews validate, including the semantically false one. This preserves the distinction between valid projection/binding and semantic entailment. See [mapping analysis](iteration-12-7098e696/mapping-analysis.md).

The smoke fails after two completed revise verdicts, without provider error. Its final comparison S is literally mapped by both web S+T and book S, yet exact-string grouping presents separate single-source groups and the reviewer falsely demands the already present contribution. I verified this deterministic projection omission and saved both full canonical passages plus exact declarations in [overlap evidence](iteration-12-7098e696/smoke12-overlap-evidence.json). The additional conclusion T remains web-only and separately reviewable; a projection correction cannot automatically grant it book support. The quotation is complete and bound. Root assigned a separate authorized design/implementation after freeze; no repair was applied to the recorded run. See [smoke analysis](iteration-12-7098e696/smoke12-analysis.md).

Farm's explicit grounding is a materially misleading native pass. I independently read the released claim, final audit and both canonical records: declared lines1133–1158 contain Squealer's leadership/sacrifice argument, wrong-decisions warning and Jones/debate rationale; only the available but undeclared1116–1143 record contains the dogs silencing porkers, sheep ending discussion and Afterwards Squealer chronology. The accepted final group summary falsely attributes that earlier silencing to the declared record. The requested quotation itself is correct. This actual source-borrowing defect is now recorded in `linger-55r9`; it is not an additional-evidence permission question. Farm's inferred reply uses an appropriate record and complete stopping proof, but its final review only succeeds after two saved content-filtered partial attempts; the filter's cause remains unknown.

Other native analyses remain attributed to their authors: Douglass inference regresses through malformed proof/inventory rather than a gold mismatch; Alice explicit grounding fails mechanical draft repair; Keller's explicit mixed personal/book claim repair fails while inference and the requested quote succeed; Pinocchio's inference fails only the extra same-event support key. These differences must survive an aggregate summary. Earlier individually verified issue closures do not mean every later run is guaranteed to repeat their successful behavior.

The [release component review](iteration-12-7098e696/component-analysis.md) records the repaired actual-reader-message handoff in all twelve cases. Giant-puppy passes without a source quotation on its first draft, so that success does not directly exercise the new period-to-comma retained-quote branch. Pigeon-serpent instead introduces a new partial quotation during revision, outside the intentionally limited prior-quote guard; identity-theme retains an unmapped synthesis and wrong-record memory/recitation claim. Those genuine repair failures remain even before resolving the separate interpretation/range question. Exact-copy probes verify their locations without pretending to prove semantics.

The completed trail and issue notes preserve the failed complete-set gate and pending human expectations. No adopted source allowlist, gold, hidden retry or budget reset was changed in this audit. The next repair remains separately accountable. Remote Logfire visibility is still unverified. Attention for the overall reviewer: inspect Farm's accepted undeclared-source claim, the missing-joint false approval, the failed mapping call's absent diagnostics, and smoke's overlapping declarations; these are not resolved by the headline pass counts.

### Post-batch12 diagnostic correction and independent quote review

Root authorized the mapping adapter's lost-failure-transcript correction after the freeze ended. I implemented it in the current `run_live.py` and its colocated adapter test, leaving historical artifacts unchanged. The adapter now uses the existing traced-run/SceneTranscriptRecorder path for the same payload and candidate-review options. Failed attempts retain sanitized messages and known failure category/status/kind. A fresh SDK RunUsage accumulator preserves available counters; `usage_complete=false` marks failures, and absent known usage remains null. No raw exception body, message, model name or thinking part is copied into the new diagnostics. Existing grading and configured retries are unchanged.

Three real FunctionModel regressions failed before the fix on absent diagnostics. Afterward the four-case invalid-output exercise still makes exactly12 model attempts under the existing repair budget, while HTTP/generic-error exercises make four; captured retries,503 classification, partial usage and redaction all pass. The adapter plus adjacent transcript/mapping checks pass **19 tests and18 subtests**. The batch driver passes **8 tests and8 subtests**. These are offline checks, not another reservation or recovered evidence about batch12's missing attempt. I authored this diagnostic fix; root/another reviewer remains responsible for independent code review.

Separately, I independently read root's new first-draft/revision quotation-edge check, the canonical-validity cache and the all-current-declarations binding exception. **47 quote-focused tests passed**. No verified authority expansion or blocker was found. The helper creates a repair obligation only around an explicitly declared, canonically valid quotation, and the caller suppresses that heuristic if any current authorized declaration already binds the complete visible occurrence. Thus a shorter sibling declaration does not falsely invalidate a complete canonical binding. Word changes and ambiguous use patterns remain under independent review. The heuristic does not assign a source, normalize canonical words, approve semantic attribution or replace the final quote audit. Source edits owned by this reviewer are stable; no live run beyond12 was reserved here.

## Independent audit — iteration 13 (2026-09-18)

Verified all **78** indexed native reports have the exact ordered Scene coverage from their saved evaluation and an existing rendered Markdown companion. Batch 13's before/after manifests are identical across **211** files. All five current scenario Backstory, Ground truth, and adoption hashes match their saved native summaries. Recounted **13/18 native Scenes** and **14/20 judgments**. The summary correctly identifies a targeted selection, not a complete evaluation set; selected/all-pass remain false. The ledger has **13/20** reservations. No additional provider calls or budget reservation were made for this audit or the subsequent comparison preparation.

Read the completed decision row, expectation review, root's `run-comparison-12-13.md`, and the native report review coverage. The same-selection comparison is correctly 11/18 to13/18 and does not compare the full19/29 directly with13/18. The application changes between runs and unchanged manifests within runs are distinguished. The mapping comparison preserves authoritative inputs while acknowledging changed derived tables. No verified overclaim was found in that comparison explanation.

Independently reviewed every mapping attempt and the full smoke input/candidate/review/source chain; wrote and read `iteration-13-4d9bca6b/mapping-analysis.md` and `smoke13-analysis.md`. Mapping4/4 is credible after bounded mechanical repairs: both negative controls initially falsely approve borrowed support, then correctly reject after source-local excerpt/inventory feedback. All four final reviews revalidate against recorded inputs. Smoke declines after revise/revise, with successful provider exchanges: its hybrid declaration switches web-only to book-only and never supplies both. The direct-only groups are accurate; this run does not exercise nontrivial projected overlap. Both smoke reviews revalidate; the final visible source quote is canonically complete.

The completed-execution decision row honestly marked release semantic review pending at its timestamp. `component-analysis.md` has since landed and was read in full. It records10/12 strict,11/12 released, and no saved provider failures. **Attention:** Father William's valid title is inconsistently labeled a source quote, then Muse adds an unsupported genre label and declines despite correct retrieval. Pigeon's released opening attributes neck growth to a declared passage that discusses the neck but does not narrate that growth; the required source is another record. FutureCat avoids road advice but its wording may imply the event occurs later. These are not erased by mechanical passes. Identity-theme's actual mappings now repair the prior source defects; its strength/expected-range difference remains unadopted human judgment. Pool and Pigeon supply actual saved evidence of the new quote-edge check; no claim of live overlap-projection or whitespace-hint coverage is made.

Farm's inference failure remains filtered, truncated boundary output before accepted structure, distinct from an incorrect granted ceiling or an extra-proof expectation failure. Keller4/4 is reviewed as credible with an unverified HTML-display limitation. Alice's extra-proof mismatch remains a strict failure pending the existing human expectation decision. No ground truth or historical grade was changed; no commit or push occurred. Remote Logfire visibility remains unverified.

Final trail follow-up: read the appended `iteration-13-review` row at03:03:52Z. It explicitly supersedes the earlier pending-release-review status and correctly retains10/12 strict versus11/12 released, the Pigeon concern, Father William failure, source unchanged,13/20 consumed, and no iteration14 reservation. The row and index agree with the checked artifacts. Root's comparison preparation remains a separate proposed experiment, not a production model change: twelve exact saved inputs, two approved-only target models,24 top-level reviews, one future shared reservation, and no semantic all-pass inference. The saved offline validation record reports21 tests plus18 subtests; no tests or provider calls were repeated for this trail follow-up.

## Independent audit — iteration 14 (2026-09-18)

Completed the full final audit in `iteration-14-76df3f36/independent-audit.md` and read it back. Recounted25/29 native Scenes and27/32 judgments;12/12 direct,10/12 release,4/4 mapping and smoke failure agree with the completion trail. Verified220 identical source hashes, all24 current scenario/adoption hashes, all86 ordered native review/rendered artifacts,14 completed Luna reservations and the successor approved menu. Explicit-human-instruction adoption accurately carries user approval for items2/3 only; Sol comparison remains unapproved/unexecuted. Required anchors, safe ceilings and historical bytes remain preserved. Full checkpoint1,812+648 and focused18+139 are distinguished from this offline artifact audit.

All16 accepted release reviews revalidate against original inputs, all reader messages arrive intact, and all source quotations in actually released replies bind completely to literal source text. `release-binding-review.json` distinguishes rejected candidate quotes and drink-me's preexisting blank-line representation. Attention remains Pigeon's false ambiguous grant, Farm's filtered incomplete inference, Roses source/causal concerns, release quotation classification/adaptation failures, and smoke's repeated private it/he copying error. All mapping negatives needed repair; their final4/4 does not imply correct first judgments. Source/model defaults were not edited, no extra live call or reservation was made, and the all-pass commit/push gate is still unmet.
