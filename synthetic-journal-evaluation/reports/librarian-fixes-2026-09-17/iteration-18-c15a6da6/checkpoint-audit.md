# Independent checkpoint recovery audit

This is a new audit performed after the user selected iteration 18 for commit. It does not amend the original live results or represent another provider run.

The reviewer independently recomputed SHA-256 digests for every path in the saved iteration 18 manifest. **All 222 files match exactly.** The nine replaced versions remain in the local preservation archive, as does the later boundary-inventory regression file. The six pre-recovery digests in the recovery method receipt match the archived later files.

The proposed commit inventory contains 272 files before adding new audit receipts and validation logs. The initial 274 existing, unique paths were inspected; the unused, unapproved model-comparison experiment and its test file were subsequently excluded. That experiment was not part of iteration 18's batch. The inventory covers the implementation, contracts, documentation, evaluation fixtures, tests and iteration 18 evidence. It includes no iteration 19–21 artifact paths, preservation-archive paths, Beads interaction export, credential files or environment files. A targeted content scan found no recognized provider keys, GitHub tokens, private-key headers or URLs containing passwords. This is a bounded scan, not a guarantee that arbitrary sensitive strings cannot exist. The retained model exchanges are synthetic evaluation evidence; duplicated progress and native evaluation payloads make the evidence archive comparatively large.

The checkpoint document accurately reports 26/29 native Scenes, 28/32 judgments, 12/12 direct retrieval, 12/12 answer release, 4/4 claim mapping and a passing smoke test. Its open issues match the independently reviewed iteration 18 artifacts: Douglass inventory repair, Farm's missing mandatory continuity anchor and filtered review, Roses review/mapping and pending expectation adoption, Pinocchio's undeclared interpretation, and review inconsistency. Later speaker-attribution, workplace-concession and excerpt-limit findings are explicitly identified as later observations.

The backend, evaluation, contract, notebook and documentation review established no additional blocking defect. Root reported that an expanded check included eight failures from stale saved inputs in the excluded comparison experiment (ten experiment tests total), plus five review-server tests blocked by sandbox loopback restrictions. The fresh checkpoint-only run excludes that experiment and permits loopback. Its result belongs in its own log and recovery receipt; this audit does not claim to have rerun or independently verified that result. The comparison experiment follow-up is tracked in `linger-9oqp.22`. The receipt now labels archived digests `before_recovery_sha256`. No runtime files, adopted expectations or provider state were changed by this reviewer.

**Attention:** The checkpoint has known live failures and a potential misleading pass. The user's explicit selection authorizes this commit despite the earlier all-pass condition; it does not establish that the checkpoint is free of those issues. No push was requested for this recovery.
