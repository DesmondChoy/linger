# Final iteration 20 mapping and smoke review

**Mapping passes 1/4; smoke fails before invocation.** Provider429 interrupts both components. These are retained failures within the final authorized batch, not retries to be silently replaced. Artifact-only review under source freeze; no provider calls or source/expectation changes.

## Mapping

Trace `01a0b4509e32afdfe54ba81bd6f324de`. Inspected all four original inputs, source records, returned model messages, validator repair, status and usage. Inputs remain identical to iteration 19. Only the correct-record control returns a final typed review; it independently revalidates against the exact input.

| Control | Recorded result and semantic assessment |
| --- | --- |
| Wrong record | HTTP429 before a returned model judgment. No observed support judgment or repair outcome. |
| Correct record | Credible first-response pass. The authentic later source directly establishes Alice's memory difficulty and verse coming out differently. It preserves the speaker and content, with a continuous exact private excerpt. |
| Missing joint contribution | First output correctly identifies the absent verse contribution and requests the later mapped record, but is mechanically invalid: it duplicates the same member once as contributing and once as noncontributing, and stitches dialogue across omitted narration into a nonliteral excerpt. The existing validator rejects both faults. The bounded repair receives429, so no valid final review exists. |
| Complete joint contribution | HTTP429 before a returned model judgment. No observed joint-support verdict or attribution-consistency repair. |

The missing-joint first output's intended revise judgment is sensible, but it is not an accepted review. Its extra `unresolved_evidence` finding redundantly describes the same missing declaration rather than another missing canonical record. That distinction and its duplicate member must not be hidden by counting the rough conclusion as a pass. The correct-record control has one recorded successful request. Failed calls without usage remain unavailable, not zero; the missing-joint recorded successful first response does not make the interrupted logical call's usage complete.

No per-member completeness relabeling, automatic approval or candidate rewrite occurred. The three interrupted controls cannot demonstrate whether repair20's actor/modality wording helped. This batch does not override iteration19's credible first-output4/4; it records a separate outcome with provider interruptions and one malformed intermediate review.

## Smoke

Run `d46542fe46304e6b8467383d13f6d976`; trace `01a0b4507f27f70f64ecb1e1428b5047`. The diagnostics contain one exchange: Prove emotional-boundary preflight fails with HTTP429 and no returned assessment. Its model messages contain only the request; output and usage are unavailable.

The application releases its generic safe fallback. The evaluator reports `serendipity_not_observed` at invocation and leaves every later stage not reached. That is the downstream observable symptom; the recorded cause is the preflight provider error, before Muse, Librarian or Serendipity runs. No outside page is selected, no book reply is drafted and no candidate-review semantic judgment exists to assess. The failure is not evidence of a new retrieval or essay-selection defect, nor a successful deliberate decline.

Attention: reduced concurrency to three suites does not eliminate429 in this batch and does not identify the provider's underlying limit or root cause. No further live reservation is available after iteration20. Any final assessment must retain missing observations and the failed grades rather than borrowing successful judgments from an earlier batch.
