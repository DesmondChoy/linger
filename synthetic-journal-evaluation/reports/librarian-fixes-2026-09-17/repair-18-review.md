# Repair18 validation

Prove now requires whole-claim attribution findings to agree with a supported claim audit when every member contributes. The model may correct its audit, relocate an independent quotation defect, or withdraw a mistaken finding. No automatic pass, extra retry or source relaxation. Shared declaration wording explicitly permits collective support. Five intended failures preceded the fix;389 nearby tests plus58 subtests passed. Independent review found no blockers and passed99tests+10subtests, including repair paths that preserve rejection.

Direct evaluation now saves sanitized per-case agent transcripts from the unchanged notebook helper, including typed-failure traces and partial rows on unexpected exceptions. Two new regressions failed before implementation and21 nearby tests passed. Root inspected the finally-path, single call, unchanged evaluation and scope. This changes observability, not grades or retrieval.

Batch concurrency defaults to6, configurable1–12; all12 suites still execute exactly once in one budget reservation. A synchronized offline test verifies queuing/one reservation without touching the live ledger;9driver tests+8subtests passed. This reduces peak requests after observedHTTP429; the stored provider category is insufficient to establish its cause, so success is not assumed.

Muse guidance preserves reported-versus-known information in proposed speech and requires hypothetical comparisons to isolate the factor rather than infer motives from confounded/reversed alternatives. Independent review found no case-specific overreach.

Full suite passed1896tests+670subtests in41.83seconds. After that checkpoint, a prompt-only clarification aligns complete quotation coverage with the existing binder: all interior characters are required, both outer display marks are not. Focused quotation/support/review checks then passed95tests+10subtests. No deterministic quotation rule changed. git diff --check clean. Live18 follows on the same Luna medium model and existing retries; Roses adoption remains pending.
