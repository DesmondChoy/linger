# Repair after iteration 6

The sixth full batch remains frozen during analysis. No runtime change below is applied until the batch records completion and unchanged source hashes.

## Private boundary source binding

Pigeon and Douglass selected the right record and the right words, but their private proof excerpts flattened canonical line wrapping. The exact-character check then exhausted the existing structural repair and withheld inference. Douglass now names the actual Chapter 6 teaching prohibition rather than the unrelated Chapter 3 selling passage observed in iteration 5.

Rename the private boundary anchor `exact_quote` to `source_excerpt`. Compare the excerpt as a substring of its named record after normalizing whitespace runs on both sides. Preserve all words, capitalization, punctuation, and markup. Keep unknown-ID, duplicate, missing-anchor, occurrence, memory and application grant checks. Do not alter public Muse quotation validation, source text, evidence IDs, repair budgets or adopted expectations. Test line wrapping acceptance and rejection of changed words, punctuation, case, and mismatched source IDs.

## False emotional refusal

The Farm inference Line contains ordinary guilt about challenging a project lead. The preflight correctly returned `continue_reflection`; Muse nevertheless withheld the requested book answer because the reader felt guilty, and Provenance passed that refusal with `emotional_boundary_decision=not_required`.

Clarify the existing Muse policy: an ordinary feeling, including guilt or self-doubt, supports reflection; length and combined literary/personal questions do not establish intense distress. Judge the complete message, preserving the stop for a clear current first-person inability to cope or intense distress. Do not introduce keyword classifiers or treat examples as unconditional exemptions.

Clarify the existing review policy: a visible false distress refusal is a repairable `emotional_policy_violation` in `candidate.response`, with `response_decision=revise` and `emotional_boundary_decision=not_required`. Use the existing single revision. A wrong no-memory reason alone is not proof that the response needs revision. Preserve independent review and the application-owned fixed response for genuine distress. No new role, decision envelope, retry or model change is needed.

Independent review: runtime_evidence agreed with the scope and corrected the proposed wording from generic rejection to explicit `revise`, since literal `reject` would bypass the normal repair path. Live verification remains necessary; offline fixtures cannot prove semantic classification.
