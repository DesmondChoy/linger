# Repair 16 review

The interrupted fifteenth batch remains failed/incomplete. No missing live result is inferred and no old grade is rewritten.

Prove now rejects a narrow contradiction: a complete claim is marked supported, every mapped source contributes, but an exact whole-claim unsupported finding still rejects it. The bounded repair allows either correcting the audit or removing the finding. It never automatically approves a response. Five intended failures preceded implementation;16 new tests pass, and229 adjacent tests plus39 subtests pass after correcting a contradictory legacy mock without changing assertions. An independent collaborator approved this scope. Partial and broader findings remain outside the narrow detector.

Muse now receives complete quoted reply text and exact declaration/reply differences when one canonical source span is unambiguous. Lookup can locate a Markdown-prefixed occurrence, but acceptance still requires literal canonical text. All four saved Pool15 attempts remain rejected unchanged; the new diagnostics explain each defect.85 tests plus9 subtests passed; an independent collaborator also passed74 nearby tests plus9 subtests and found no blocker. This does not guarantee the live model follows the feedback.

The evaluator now records private boundary evidence and memory support only when the application actually grants a chapter or passage scope. A denied candidate remains in the raw transcript for diagnosis. The regression failed before the fix;112 focused tests pass, including retained accepted support. Root inspected the outcome gate, both support fields and regression. Existing ground-truth expectations remain unchanged.

No retry-budget, model, source-evidence acceptance or human adoption changes are included. A full integration checkpoint is recorded separately in local-tests-repair-16.log before the next live reservation.

The first complete integration run passed1854 tests and648 subtests, with one additional contradictory mock in the offline live-adapter test. Its expected outcomes and assertions were preserved while the mock audit was made coherent. All four adapter tests then passed. A final complete rerun follows in local-tests-repair-16-final.log.

Final complete check:1855 tests and648 subtests passed in41.78 seconds. Independent decision-trail audit verified222 unchanged historical source hashes, reservation15 digest, adopted scenario hashes and87 existing report links. Missing iteration15 outcomes remain unknown. Live repair16 effectiveness is pending.
